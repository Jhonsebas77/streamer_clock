part of com.jsob.flutter_clock.ui.styles;

TextStyle textStyle({
  Color color = Colors.black,
  double fontSize = 18,
}) =>
    TextStyle(
      fontWeight: FontWeight.bold,
      fontSize: fontSize,
      color: color,
    );
